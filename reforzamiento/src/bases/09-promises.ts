/*
const myPromise = new Promise((resolve, reject) => {
    setTimeout(() => {
    //! yo quiero mi dinero!!
    resolve('mi amigo se perdio');
    }, 2000);//2 segundos
});

myPromise.then((myMoney) => {
        console.log(`Tengo mi dinero ${ myMoney }`);
    }).catch(reason => {
        console.warn(reason);
    })
    .finally(() => {
        console.log('Pues a seguir con mi vida');
    });

*/
